{
  "name": "course-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/course-service/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["{projectRoot}/src/**/*"],
      "outputs": ["{workspaceRoot}/dist/apps/course-service"],
      "options": {
        "command": "webpack-cli build",
        "args": ["--node-env=production"],
        "cwd": "apps/course-service"
      },
      "configurations": {
        "development": { "args": ["--node-env=development"] }
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "parallel": true,
        "commands": [
          "webpack-cli build --node-env=production --watch",
          "node --watch ../../dist/apps/course-service/main.js"
        ],
        "cwd": "apps/course-service"
      },
      "configurations": {
        "development": {
          "commands": [
            "webpack-cli build --node-env=development --watch",
            "node --watch ../../dist/apps/course-service/main.js"
          ]
        },
        "production": {
          "commands": ["webpack-cli build --node-env=production", "node ../../dist/apps/course-service/main.js"]
        },
        "debug": {
          "commands": [
            "webpack-cli build --node-env=development",
            "node --inspect-brk=9232 ../../dist/apps/course-service/main.js"
          ]
        }
      }
    },
    "test": { "options": { "passWithNoTests": true } }
  }
}
